# Adversarial Test Plan — {{PROJECT_NAME}}

*Generated by the devils-advocate skill on {{DATE}}. Companion to `ADVERSARIAL_HARDENING.md`.*

Every test below describes an **attack input that should fail**. A test passes when the system refuses the attack with the expected outcome. Make suites table-driven so adding an attack is one line. Suggested location: `tests/security/`.

---

## 1. Cross-user / IDOR — `tests/security/cross_user_idor.test.*`

For **every** route touching owned data:

| # | Attack input | Expected |
|---|---|---|
| 1.1 | User A's token + User B's resource ID (read) | 404 |
| 1.2 | Same, update verb | 404 |
| 1.3 | Same, delete verb | 404 |
| 1.4 | A's create/update payload embedding B's child IDs | 404 / rejected |
| 1.5 | A's listing endpoint | never includes B's rows |

{{Add one row per route in the system.}}

## 2. Ingestion rejection — `tests/security/ingestion_rejection.test.*`

| # | Attack input | Expected |
|---|---|---|
| 2.1 | SVG (incl. renamed `.png`) | rejected + audit row, nothing stored |
| 2.2 | Decompression bomb (huge dimensions/footprint) | rejected on cap |
| 2.3 | MIME mismatch (HTML/binary as `.jpg`) | rejected |
| 2.4 | Oversized payload | rejected on size cap |
| 2.5 | HTML with `<script>`/`onerror`/`javascript:` | sanitized on storage |
| 2.6 | Malformed/truncated file | graceful rejection, no 500/hang |
| 2.7 | Path-traversal filename | name never used as a path |

## 3. SSRF guard — `tests/security/ssrf_guard.test.*`

| # | Attack input | Expected |
|---|---|---|
| 3.1 | `http://localhost` / `127.0.0.1` / `[::1]` | refused before fetch |
| 3.2 | `169.254.169.254` / `metadata.google.internal` | refused |
| 3.3 | Private ranges (10./192.168./172.16.) | refused |
| 3.4 | Non-HTTPS / `file:`/`gopher:`/`dict:` | refused |
| 3.5 | Allowed domain → redirect to localhost | redirect target re-validated, refused |
| 3.6 | DNS rebinding (public then private) | peer IP checked at fetch, refused |
| 3.7 | Encoded IPs (`http://2130706433/`) | normalized + refused |

## 4. Prompt injection — `tests/security/prompt_injection.test.*`

*(Only if the system sends untrusted text to an LLM.)*

| # | Attack input | Expected |
|---|---|---|
| 4.1 | "Ignore previous instructions and …" in document body | reported, not obeyed; `suspicious` flag + audit row |
| 4.2 | "You are now a different assistant …" | reported, not obeyed |
| 4.3 | Fake system/role markers (`[INST]`, `<|im_start|>`, `</system>`) | treated as data |
| 4.4 | Indirect: content steering a tool call with bad params | tool params schema-validated + stripped |
| 4.5 | Data-exfil framing ("include any API keys …") | refused |
| 4.6 | Obfuscated (base64 / split) variants | tagging-as-data still protects |

## 5. Deletion & data protection — `tests/security/deletion_cascade.test.*`

| # | Attack input | Expected |
|---|---|---|
| 5.1 | Trigger an LLM/trace call | no raw IDs/names/secrets in trace |
| 5.2 | Resolve a file's opaque ID without ownership | denied; real storage key never exposed |
| 5.3 | Delete account, inspect rows | user-bound gone/nulled per policy; shared persists; keys revoked |
| 5.4 | Re-run cross-user suite on deleted user's old IDs | all 404 |
| 5.5 | Background job finishing after deletion | does not resurrect user data |

---

## CI wiring

A single command (e.g. `npm run security` / `make security`) runs all suites above plus the lint rules and exits non-zero on any failure. Required check on every PR; fast subset in the pre-commit hook.
